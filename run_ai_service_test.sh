#!/bin/bash

# إجراء لاختبار خدمة الذكاء الاصطناعي المعتمدة على REST API

echo "اختبار خدمة الذكاء الاصطناعي (REST API)..."
echo "==========================================="

# التحقق من وجود مفتاح API للذكاء الاصطناعي
if [ -z "$OPENAI_API_KEY" ]; then
    echo "تحذير: المتغير البيئي OPENAI_API_KEY غير موجود."
    echo "سيتم تشغيل الاختبار في وضع المحاكاة."
else
    echo "تم العثور على مفتاح OpenAI API."
fi

# تنفيذ اختبار واجهة REST API
python test_ai_rest.py

# التحقق من نتيجة الاختبار
if [ $? -eq 0 ]; then
    echo "تم إكمال اختبار خدمة الذكاء الاصطناعي بنجاح."
else
    echo "فشل اختبار خدمة الذكاء الاصطناعي."
fi

echo "==========================================="