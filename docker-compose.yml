version: '3.8'
services:
  btec-system:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      # أمثلة على متغيرات بيئة إن وجدت:
      # FLASK_ENV: development
      # DATABASE_URL: sqlite:///./btec.db